services:
  - type: web
    name: lung-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app_lung.py --server.port=10000 --server.address=0.0.0.0"